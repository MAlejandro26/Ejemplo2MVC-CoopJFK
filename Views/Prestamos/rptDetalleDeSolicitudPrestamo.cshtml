@using DevExpress.Web.Mvc
@using DevExpress.Web.Mvc.UI
@model DevExpress.XtraReports.UI.XtraReport
@{
	ViewBag.Title = "Detalle de Solicitud Prestamos";
}

<h2>Detalle de Solicitud Prestamos</h2>

<p>
	<a class="btn btn-success" href="@Url.Action("Reporte","Prestamos")">Atrás</a>

</p>

@Html.DevExpress().WebDocumentViewer(settings =>
{
	settings.Name = "Viewer_Detalle_Solicitud_Prestamos";
}).Bind(Model).GetHtml()
