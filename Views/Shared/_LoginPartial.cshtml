@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()
        <ul class="nav navbar-top-links navbar-right pull-right">
            <li>
                @*<a href="@Url.Action("WidgetView", "Home")" title="Widget View" class="links">
                        <img src="~/Content/jfk-profile.jpg" alt="user-img" width="36" class="img-circle"><b class="hidden-xs">User.Identity.GetUserName()</b>
                    </a>*@
                @Html.ActionLink("Hola "+User.Identity.GetUserName()+" !", "Index", "Manage", routeValues: null, htmlAttributes: new { @class = "profile-pic", @style = "font-weight:bold" })
            </li>
            <li style="padding-right:10px">
                <a href="javascript:document.getElementById('logoutForm').submit()">
                    <b class=""><span><i class="fa fa-sign-out" aria-hidden="true" style=""></i>Cerrar Sesion</span></b>
                </a>
            </li>
        </ul>
    }
    //htmlAttributes: new { title = "Manage" }
}
